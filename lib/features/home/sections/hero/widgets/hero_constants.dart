class HeroConstants {
  HeroConstants._();

  // Background
  static const backgroundWidthFactor = 0.72;
  static const backgroundHeightFactor = 0.90;

  // Image
  static const imageWidthFactor = 0.85;
  static const imageHeightFactor = 1.05;

  static const imageRightOffset = -20.0;
  static const imageBottomOffset = -20.0;

  // Floating Card
  static const professionCardRightOffset = 350.0;
  static const professionCardBottomOffset = 150.0;

  static const nameCardRightOffset = 100.0;
  static const nameCardBottomOffset = 250.0;
}
