class HeroConstants {
  HeroConstants._();

 // Background
  static const backgroundWidthFactor = 0.72;
  static const backgroundHeightFactor = 0.90;

  // Image
  static const imageWidthFactor = 0.85;
  static const imageHeightFactor = 1.05;

  static const imageRightOffset = -20.0;
  static const imageBottomOffset = -20.0;
}
